---
layout: layouts/base.njk
title: Damon Kelley
description: "Writing, building, and thinking about how humans and software shape each other."
---
<div class="h-feed">
  <ul>
    {%- for item in collections.feed -%}
    <li>
      {%- if item.data.title -%}
      <article class="h-entry">
        <a href="{{ item.url }}"><time class="dt-published" datetime="{{ item.date | htmlDateString }}">{{ item.date | readableDate }} — {{ item.content | readingTime }}</time></a>
        <h2 class="p-name"><a class="u-url" href="{{ item.url }}">{{ item.data.title }}</a></h2>
        <p class="p-summary">{{ item.data.description }}</p>
        <a class="read-more" href="{{ item.url }}">Read more →</a>
      </article>
      {%- else -%}
      {% set entryUrl = item.url %}{% set entryDate = item.date %}{% set entryContent = item.content %}{% set entryStatus = item.data.status %}{% set entryFederate = item.data.federate %}{% set entryPhotos = item.data.photos %}
      {% include "note.njk" %}
      {%- endif -%}
    </li>
    {%- endfor -%}
  </ul>
</div>
